SELECT type, sign, donation_count
FROM (
    SELECT DR.type,DR.sign,COUNT(*) AS donation_count
    FROM Donation DN
    JOIN Donor DR ON DN.person_id_donor = DR.person_id
    WHERE DR.gender = 'F'
    GROUP BY DR.type, DR.sign
    ORDER BY donation_count DESC
)
WHERE ROWNUM = 1;
